package de.lulonaut.bot.commands

class Aliases {
    enum class LeaderboardAliases {
        Leaderboard, leaderboard, top10, topMessages, topmessages
    }

    enum class LookupUserAliases {
        check, lookup, checkuser, checkmsg
    }

    enum class DeleteMessagesAliases {
        deleteMessages
    }
}