package de.lulonaut.Bot.commands;

public class Aliases {

    enum LeaderboardAliases {
        Leaderboard,
        leaderboard,
        top10,
        topMessages,
        topmessages
    }

    enum LookupUserAliases{
        check,
        lookup,
        checkuser,
        checkmsg,
    }
}
